package up2date.up2dateSecurity.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import up2date.up2dateSecurity.model.PlaceWorking;

public interface PlaceWorkingRepository extends JpaRepository<PlaceWorking, Integer> {
}
